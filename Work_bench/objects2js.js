/* 
objects in java script

Boolean can be Objects(if defined with new keyword).
Number can be objects (if defined with new keyword).
Strings can also be objects (if defined with new Keyword).
Dates are always abjects
Math are always objects
Regular Expressions are also Objects
*/

var str1 = new String();
typeof(str1); //typeof will give the information of the variable type.

document.writeln(typeof(str1));